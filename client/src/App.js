import { BabyGenerator } from './pages/BabyGenerator';

const App  = () => {
  return (
    //Basically this is only the starting point of the application. It instantiates directly the baby generator page but in a real project we can use here the routes, etc...
    <BabyGenerator />
  );
}

export default App;
